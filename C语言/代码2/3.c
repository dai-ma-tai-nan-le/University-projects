/*#include<stdio.h>
int main()
{
    int rank(int *p1,int *p2);
    int a,b;
    scanf("%d %d",&a,&b);
    rank(&a,&b);
    printf("%d %d",a,b);
}
    int rank(int *p1,int *p2)
    {
        int t;
        if(*p1>*p2)
        {
            t=*p1,*p1=*p2,*p2=t;
        }
    }*/
/*#include<stdio.h>
int main()
{
    int max(int *p,int n);
    int a[10],i;
    for(i=0;i<10;i++)
    scanf("%d",&a[i]);
    printf("%d",max(a,10));
}
    int max(int *p,int n)
 {
     int max=*p,j;
     for(j=0;j<n;j++)
     {
         if(max<p[j])
            max=p[j];
     }
     return max;
 }*/











